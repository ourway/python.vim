# python.vim
A Python friendly vim config

## Installation
```
git clone git@github.com:ourway/python.vim.git
cd python.vim
mv ~/.vim ~/.vim.bk
mv ~/.vimrc ~/.vimrc.bk
ln -s $PWD/.vim ~/.vim
ln -s $PWD/.vimrc ~/.vimrc
```

